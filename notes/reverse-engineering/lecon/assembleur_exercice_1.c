// lire un if qui est simple.
#include <stdio.h>
int main()
{
    int x = 10;
    if (x == 10)
    {
        printf("oui\n");
    }
    return 0;
}

// Questions.

// linstruction qui fait la comparaison est cmp.

// cest jne

// jai compris on met jne afin de retourner a l'instruction return 0 en C si ce nest pas le cas on mov
